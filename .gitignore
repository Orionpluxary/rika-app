node_modules/
dist/
build/
.env
.env.local

# Nested workspace snapshot; keep the real project root only.
rika-ai-agent/

# Rika's local data — memory, activity log, conversation history.
# These contain the user's personal facts; never commit them.
server/src/data/*.json

.DS_Store
*.log
